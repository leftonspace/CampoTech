#!/bin/sh
# CampoTech Pre-Commit Hook
# Lightweight checks only — heavy audits run in CI

# Run linting on staged files only (fast)
echo "🔍 Running lint checks..."
pnpm lint

if [ $? -ne 0 ]; then
  echo "❌ Lint check failed!"
  exit 1
fi

echo "✅ Pre-commit checks passed!"
